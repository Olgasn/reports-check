import { PARAMS } from '@constants';
import styled from 'styled-components';

export const LabTaskText = styled.div`
  font-size: ${PARAMS.MEDIUM_FONT_SIZE};
  font-family: ${PARAMS.DEFAULT_FONT};
  margin-left: 4px;
  margin-right: 4px;
`;
