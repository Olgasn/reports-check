import { PARAMS, COLORS } from '@constants';
import styled from 'styled-components';

export const StudenText = styled.div`
  font-family: ${PARAMS.DEFAULT_FONT};
  font-size: ${PARAMS.MEDIUM_FONT_SIZE};
  color: ${COLORS.TEXT};
  margin-left: 10px;
`;
