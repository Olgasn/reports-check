import styled from 'styled-components';

export const GroupChipContainer = styled.div`
  display: flex;
  flex-direction: row;
  background-color: white;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
  border-radius: 6px;
  padding: 10px 15px;
  align-items: center;
`;

export const GroupDiv = styled.div`
  display: flex;
  flex-direction: row;
  flex-grow: 1;
`;

export const GroupItemsDiv = styled.div`
  display: flex;
  flex-direction: row;
  width: 10px;
`;
